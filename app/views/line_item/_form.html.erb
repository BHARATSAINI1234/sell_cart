<%= form_for(@line_items) do |f| %>
  <% if @line_items.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@line_items.errors.count, "error") %> prohibited this line_item from being saved:</h2>

      <ul>
      <% @line_items.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br />
    <%= f.number_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :cart_id %><br />
    <%= f.number_field :cart_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
Footer